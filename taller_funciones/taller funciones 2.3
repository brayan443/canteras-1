Algoritmo sin_titulo
	//ingreso al curso,consultar usuarios,resultados
	dimension nombre(8);
		Definir opcion Como Entero
		Definir i como entero;
		repetir
		escribir "1)In	gresar al curso: ";
		escribir "2)Consultar Usuarios: ";
		escribir "3)Resultados: ";
		escribir "4)salir: ";
		escribir "elija una opción (1-4): ";
		leer op;
		Segun op Hacer
			 1:
				 para i<-1 Hasta 8 Hacer
					 escribir "nombre de usuario"
					 leer nombre(i)
				 FinPara
			 2:
				verusuario(nombre,i)
			3: para i<-1 Hasta 8 Hacer
				escribir"nota del usuario"
				leer nota,nombre(i)
				si nota>=3 Entonces
					escribir "aprobo el curso"
				sino 
					escribir"no aprobo"
				FinSi
				fin para
		Fin Segun
hasta que op==2
FinAlgoritmo
subproceso verusuario(nombre,i)
	para i<-1 hasta 8 Hacer
		si nombre(i) <> "0" Entonces
			escribir " ",nombre(i)
		FinSi
	FinPara
FinSubProceso
